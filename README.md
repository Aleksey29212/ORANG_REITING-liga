
# Firebase Studio - DartBrig Pro

Это профессиональная система управления турнирами по дартсу, построенная на NextJS и Firebase.

---

## 🚀 Как отправить проект на GitHub (ОДНА КОМАНДА)

Чтобы сохранить ваш код в ваш репозиторий, выполните:

```bash
npm run github-push -- https://github.com/Aleksey29212/ORANG_REITING-liga.git
```

Скрипт сам инициализирует Git (если нужно), добавит все файлы, создаст коммит и принудительно обновит ветку `main` в вашем репозитории.

---

## Развертывание через Docker (Для VPS)

Для запуска приложения на вашем сервере (`185.219.7.95`) выполните:

### 1. Сборка образа
```bash
docker build -t dartbrig-pro .
```

### 2. Запуск контейнера
```bash
docker run -d \
  -p 80:3000 \
  -e NEXT_PUBLIC_ADMIN_PASSWORD=ваш_пароль_здесь \
  --name dartbrig-app \
  --restart always \
  dartbrig-pro
```

---

## Развертывание в Firebase App Hosting

Если вы хотите использовать инфраструктуру Google:

```bash
# Установите инструменты
npm install -g firebase-tools

# Авторизуйтесь
firebase login

# Инициализируйте проект
firebase use [ID_ВАШЕГО_ПРОЕКТА]

# Разверните
firebase apphosting:backends:deploy
```
